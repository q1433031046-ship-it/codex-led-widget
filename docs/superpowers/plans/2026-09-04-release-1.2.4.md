# 1.2.4 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the authorized 1.2.4 source and one Windows x64 installer to the existing GitHub repository.

**Architecture:** Freeze a clean source candidate, rebuild the matching installer, and verify its installed contents. Push the main branch and an annotated tag, stage a draft release, verify the uploaded asset digest, then publish and verify the public release.

**Tech Stack:** Git, PowerShell, Node.js, Electron 31, electron-builder, GitHub REST API.

## Global Constraints

- Repository: `q1433031046-ship-it/codex-led-widget`; version `1.2.4`; tag `v1.2.4`.
- Upload only `Codex-Quota-Desktop-Assistant-1.2.4-Windows-x64-Setup.exe`.
- Do not force-push or overwrite an existing tag or published asset.
- Credentials remain in memory and are never printed or committed.
- Preserve local user data; stop only the assistant's installed processes during overwrite installation.
- Automated geometry checks are not a human visual-smoothness measurement.

## Task 1: Version and release copy

**Files:** `CHANGELOG.md`, `README.md`, `USER_GUIDE.zh-CN.md`, `RELEASE_NOTES_v1.2.4.md`, `src/main/app-server-session.js`, `scripts/test-app-server-session.js`.

**Interfaces:** Consumes package version `1.2.4`; produces matching protocol metadata and public download instructions.

- [x] Align current download references and App Server client version to `1.2.4`.
- [x] Describe refresh intervals and retained history accurately, without performance measurements that were not collected.
- [x] Run `pnpm test`; require exit code 0 for all 26 scripts.

## Task 2: Freeze and package

**Files:** Existing package inputs under `src/`, `package.json`, `LICENSE`, `NOTICE.md`, `USER_GUIDE.zh-CN.md`; generated installer under `dist/`.

**Interfaces:** Consumes a clean commit; produces an installer SHA-256 and a matching installed `app.asar`.

- [ ] Commit only the release metadata changes and record `git rev-parse HEAD`.
- [ ] Run `pnpm test`, `pnpm build:dir`, and `pnpm build`; require all to pass.
- [ ] Verify packaged source equals the candidate source and record the installer size and SHA-256.
- [ ] Overwrite-install locally, restart the installed executable, and compare installed `app.asar` to `dist/win-unpacked/resources/app.asar`.

## Task 3: Remote release

**Files:** `RELEASE_NOTES_v1.2.4.md` and the verified installer; repository and release metadata on GitHub.

**Interfaces:** Consumes candidate SHA and installer SHA-256; produces a public release URL and verified remote digest.

- [ ] Read repository permissions, remote main, existing tags and release state through GitHub REST.
- [ ] Require remote main to be an ancestor of the candidate. Create an annotated `v1.2.4` tag targeting the candidate and push without force.
- [ ] Create a draft release for that tag with the release notes; upload the single installer.
- [ ] Verify peeled tag commit, asset name, size, uploaded state and digest.
- [ ] Publish the draft as the latest stable release; read it back and confirm `draft=false`, `prerelease=false`, and the intended asset.
- [ ] Download the public installer and compare its SHA-256 to the local candidate.
