# Codex 额度桌面助手使用说明书

Codex 额度桌面助手是一款 Windows 桌面悬浮工具，用于查看 Codex 服务端实际提供的 5 小时额度、7 天总额度、历史消耗、Token 用量和 API 等值费用。它会使用电脑上已经登录的 Codex 状态，不要求另行填写账号或 Token。

## 一、软件特色

- 按服务端返回的窗口时长识别 5 小时和 7 天额度；Pro 账号只有 7 天窗口时不会误显示成 5 小时额度。
- 可在设置中切换 Codex、Spark 等当前账号实际返回的额度来源。
- 圆球、横向电池、竖向电池三种仪表形态，宽高和位置均可调整。
- 窗口、卡片、左右分栏和仪表支持自由缩放，并记住位置、大小与全部显示设置。
- 支持屏幕上下左右四边磁吸；收起后将鼠标移到屏幕边缘即可唤回。
- 5 小时与 7 天消耗图可分别开启，对比理论匀速消耗和实际消耗。
- 独立统计页展示今天、本周、累计额度、Token、模型明细以及美元/人民币 API 等值费用。
- 格子消耗日历支持单月、多月连续时间线和全年视图，可切换额度、Token、美元或人民币单位。
- 颜色可随额度平滑变化，也可关闭变色或让各模块使用独立颜色。
- 检测到 GitHub 新的正式版本时会弹窗提醒，同一个新版本只提醒一次。

## 二、下载与首次运行

### 系统要求

- Windows 10 或 Windows 11，x64 版本。
- 已安装 Windows 版 Codex，并已正常登录。

### 下载

1. 打开 [GitHub Releases](https://github.com/q1433031046-ship-it/codex-led-widget/releases)。
2. 进入最新版本，下载 `Codex-Quota-Desktop-Assistant-1.2.0-Windows-x64-Setup.exe`。
3. 双击安装程序完成安装。安装后的应用会直接从安装目录运行，不会在每次启动时向临时目录解压完整程序。

当前程序没有商业代码签名。如果 Windows 首次运行时显示“未知发布者”，请先确认文件来自本仓库，然后选择“更多信息 → 仍要运行”。

应用启动后会检查 GitHub 最新正式发行版。检测到高于当前版本的正式版本时，会显示“前往下载”或“稍后处理”的弹窗；无论选择哪一项，同一个新版本都不会再次弹出，只有更高版本发布后才会重新提醒。

### 首次启动

程序启动后会先恢复一次安全布局：若旧版磁吸位置曾让窗口缩成屏幕边缘的一条细线，会关闭旧磁吸并把窗口放回主屏幕右下角。这个恢复只执行一次，会保留窗口大小、主题、卡片、消耗历史和统计数据。

随后会显示初始化窗口并自动检查本机 Codex 登录状态。已有有效登录时会直接同步；未登录时会打开 Codex 官方浏览器登录页，完成登录后自动继续，无需把密码或 Token 填进本工具。初始化窗口提供“重新尝试”和“重新打开登录页”，暂时关闭后也能继续使用上次缓存的数据。

每次启动时，上次成功额度会先标记为缓存，只有实时刷新成功后才恢复为最新状态，避免把旧的 7 天额度误当成当前值。

右下角系统托盘会出现程序图标。右键悬浮窗或托盘图标会打开扁平快捷菜单，可直接显示/隐藏、刷新、置顶、打开统计或进入完整设置窗口。

首次成功读取账号后，程序会在本机建立账号记录。账号名称、套餐类型、额度快照、消耗历史、统计和窗口偏好按账号分别保存；以后更换 Codex 登录账号，刷新成功后会自动切换到对应记录。旧版本根目录中的相关文件只会复制到首个账号档案，不会删除，便于回滚。账号记录不包含密码、认证令牌或原始账号 ID。

## 三、悬浮窗基本操作

- **移动窗口：** 按住悬浮窗的空白区域拖动。
- **改变整体尺寸：** 拖动窗口四边或四角。
- **调整左右区域：** 拖动仪表与卡片之间的分隔线。
- **调整卡片：** 拖动卡片边缘或角落改变宽高。
- **调整仪表：** 圆球或电池的四边、四角都可以拖动。
- **恢复默认比例：** 右键打开“设置”，在“窗口与卡片”或仪表设置中使用对应的重置按钮。

窗口接近正方形或关闭全部卡片时，仪表会自动适应剩余空间。重新打开软件后，窗口位置、大小、卡片组合、仪表尺寸和其他设置都会保留。

## 四、快捷菜单与设置窗口

右键菜单只保留高频动作：当前额度摘要、显示/隐藏、刷新、置顶、打开统计、选择额度来源、打开设置、语言和退出。它不再使用多层原生子菜单，因此连续操作时不会闪烁、错位或被强制重新打开。

选择“设置”会打开独立窗口。这个窗口只有一个实例；重复点击会聚焦已有窗口。所有开关和选择都会立即保存，并在顶部显示保存或错误状态。

### 1. 额度、仪表与来源

- **额度来源：** 选择 Codex、Spark 或当前账号返回的其他额度桶。切换失败时会恢复原选择。
- **服务方案与窗口：** 方案名称直接来自 Codex；5 小时和 7 天窗口依据服务端时长识别，不按返回顺序猜测。
- **不可用窗口：** 当前来源没有 5 小时或 7 天窗口时，对应卡片会隐藏，设置项会禁用并显示原因。
- **贴边磁吸与置顶：** 在设置窗口中直接切换。
- **卡片显示：** 分别控制 5 小时、7 天、消耗统计和 Token 费用卡片；总开关仍会保留各卡片原来的勾选组合。

### 2. 颜色与能量仪表

#### 颜色

- “颜色随额度变化”关闭后使用固定青蓝色。
- 开启后，剩余额度从 50% 开始平滑向警示色变化，35% 为黄色关键点，10% 为红色关键点。
- “统一颜色”按 5 小时额度控制全部模块；“各模块独立颜色”让每个模块按自己的额度变色。

#### 能量仪表

- 可关闭仪表，或选择圆球、电池皮肤。
- 电池可切换横向、竖向。
- 数据来源可选择 5 小时额度或 7 天总额度。

### 3. 5 小时与 7 天卡片

- 5 小时卡可选择显示本轮“已用”或“剩余”，也可显示上轮未用额度。
- 7 天卡可显示本周已用和上周未用额度。
- 重置时刻与重置倒计时可以分别开关。
- 两张卡的消耗图可以同时开启；开启后，图表会替代对应卡片的普通进度条。
- 不常用的周期细节收在可展开区域中，避免主设置页面过长。

## 五、磁吸使用方法

1. 在“窗口与布局”中开启“贴边磁吸 · 悬停展开”。
2. 将悬浮窗拖到屏幕的上、下、左或右边缘。
3. 鼠标离开后窗口会立即收起，只保留一小段可发现的边缘。
4. 将鼠标移回该边缘，窗口会完整展开。

左右吸附时，仪表默认自动移动到更合适的一侧；也可以在“仪表左右位置”中手动固定。只显示仪表时同样支持磁吸。横向电池吸附后会使用精简横条，只显示当前额度。

## 六、额度统计页

右键快捷菜单选择“打开额度统计页”，或在设置窗口中点击对应按钮。统计页包含：

- 今日 5 小时消耗、今日消耗总额度。
- 本周 5 小时消耗、本周总额度消耗。
- 累计额度、Token 与费用统计。
- 按模型拆分的输入、缓存输入、缓存写入和输出 Token。
- 消耗日历和模型价格设置。

### 时间范围

- “今日”在本地时间每天 00:00 重新开始。
- “本周”在本地时间每周一 00:00 重新开始。
- “累计”表示本工具能够从本机读取并持续保存的全部记录。

### 消耗日历

- **月 → 单月：** 放大的 7 列日期格。
- **月 → 多月：** 可左右拖动、跨年份连续延伸的每日格子时间线，当前月份居中突出。
- **年 → 12个月：** 每个月显示为一个汇总大格。
- **年 → 每日：** 显示全年每日小格。
- 单位可在额度百分比、Token、美元和人民币之间切换。
- 鼠标停留在格子上可以查看当天的具体消耗。

日历只能展示本工具开始记录后的历史数据，无法补回此前不存在于本机记录中的额度百分比。

## 七、Token 与费用说明

- Token 总数来自账户或本机可读取的使用记录。
- 模型明细来自本机 Codex 会话日志，因此模型统计可能晚于总 Token 的起始时间。
- 金额是按照对应模型 API 单价换算的**等值估算**，不是 Codex 订阅账单，也不代表实际扣款。
- 当部分早期 Token 缺少模型明细时，程序会依据已识别部分的平均费用估算完整总价，因此会显示约等于金额。
- 人民币金额使用公开 USD/CNY 汇率换算；暂时无法取得汇率时会保留美元结果。

在统计页点击“模型与价格设置”，可以重新扫描本机模型、刷新官方价格、手动修改单价或恢复官方自动更新。未来 Codex 出现新模型时，也会自动进入模型列表。

## 八、常见问题

### 一直显示“正在读取”或没有额度

1. 确认 Windows 版 Codex 已安装并登录。
2. 右键程序，点击“刷新额度”。
3. 如果 Codex 刚完成登录，可退出并重新打开本工具。

刷新失败时，程序会保留上一次成功读取的缓存并明确标记为旧数据，不会先清空数据或突然变成错误颜色。设置页的“复制诊断信息”只复制版本、额度来源、窗口时长、百分比和刷新状态等白名单字段，不会包含认证信息、账号名称、账号标识或本机路径。

### Pro 账号为什么只显示 7 天额度

这是正常情况：应用会忠实显示当前额度来源实际返回的窗口。若 Codex 来源只返回 10080 分钟（7 天）窗口，5 小时卡会自动隐藏；如果 Spark 来源同时返回 300 分钟和 10080 分钟窗口，可在设置页切换来源查看。服务端返回的已用百分比会直接使用，不会再因套餐名称或数组位置被二次换算。

### 统计数据为什么刚开始不完整

额度历史只能从本工具开始记录后逐步积累；模型明细也取决于本机仍可读取的 Codex 会话日志。使用时间越长，日历和统计越完整。

### 金额为什么和实际账单不同

这里展示的是按 API 公开价格计算的等值估算，用来观察使用规模，不是 Plus、Pro 或其他订阅方案的真实收费。

### 找不到悬浮窗

查看右下角系统托盘，右键程序图标后选择“显示/隐藏悬浮窗”。如果开启了磁吸，也可将鼠标移动到屏幕四边寻找收起后的边缘。

## 九、隐私说明

- 程序使用本机已有的 Codex 登录状态，不要求输入或保存认证 Token。
- 程序会按你同意记录的可见账号名称建立本地账号档案，用来隔离不同账号的额度、历史、统计和窗口偏好；不会保存原始账号 ID。
- 额度、Token 历史、统计快照和界面设置保存在当前电脑。
- 不会上传你的额度或 Token 使用数据。
- 外部请求仅用于读取公开模型价格与 USD/CNY 汇率，不会附带个人用量数据。

## 十、反馈问题

遇到问题时，可以在仓库的 [Issues](https://github.com/q1433031046-ship-it/codex-led-widget/issues) 页面提交。建议附上：

- Windows 版本。
- Codex 额度桌面助手版本。
- 问题发生前的操作步骤。
- 不包含账号隐私的截图。
